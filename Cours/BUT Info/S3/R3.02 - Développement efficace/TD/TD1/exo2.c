#include <stdio.h>
#include <stdlib.h>

int main() {
    // (*p)++ veut dire qu'on ajoute 1 à la valeur de la variable pointé par p.
    // *p++ veut dire qu'on ajoute 1 à l'addresse qu'il y a dans p et non pas la variable pointé (cela n'a aucun sens).
    // *(p++) la même chose que celui du dessus.
    return 0;
}